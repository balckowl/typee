import React from 'react'

const GameStrat = ({ setIsStart }) => {

    return (
        <div>
            <button onClick={setIsStart}>GameStart</button>
        </div>
    )
}

export default GameStrat